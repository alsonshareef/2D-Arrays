const {
    add,
    subtract,
    multiply,
    divide
} = require('./functions')

let matA = [[1, 2], [3, 4]]
let matB = [[5, 6], [7, 8]]

// All operation functions
    // add(matA, matB)
    // subtract(matA, matB)
    // multiply(matA, matB)
    // divide(matA, matB)